<script>
  <connectionPool var="pool" minConnections="" maxConnections="" idleLife=""/>

  <networkClient var="client" remoteAddress="" remotePort="" pool="$pool" threads="" connectTimeout="" keepAliveTimeout=""/>
  <networkServer var="server" localAddress="" localPort="" threads="" timeout=""/>
  
  <run remote="$client">
  </run>


1. Server compessor (compress) is always used from the server model thread.
2. Client compressor (decompress) is always used from I/O thread. 
