Rabbit MQ Integration

+ Eliminate use of IFeatured interface because it places a hidden dependency on subclasses of XioPeer.


AMQP Registration Flow

+ Agent creates queue with registration name and consumer
+ Agent sends registration request to server
+ Server registers agent
+ Server sends agent requests to agent queue
